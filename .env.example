# Copy to .env and fill in. All values optional — you can also enter the key in the app sidebar.
# API_KEY=your_key_here

# Optional overrides (leave blank to use provider defaults set in the app):
# PROVIDER=gemini
# MODEL=gemini-2.0-flash
# BASE_URL=https://generativelanguage.googleapis.com/v1beta/openai/